<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper SYSTEM "path/mybatis-3-mapper.dtd">
<mapper namespace="co.yedam.review.mapper.ReviewMapper">
	<select id="selectList" resultType="co.yedam.review.service.ReviewVO">
		SELECT * FROM REVIEW ORDER BY USER_ID
	</select>
	
	<insert id="insert" parameterType="co.yedam.review.service.ReviewVO">
		INSERT INTO REVIEW(REVIEW_DATE, REVIEW_STAR, REVIEW_IMAGE, REVIEW_CONTENT)
		VALUES
	</insert>
</mapper>
