<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper SYSTEM "path/mybatis-3-mapper.dtd">
<mapper namespace="co.yedam.users.mapper.UserMapper">

	<select id="selectList"
		resultType="co.yedam.users.service.UserVO">
		SELECT * FROM USERS
	</select>

	<insert id="insertUser"
		parameterType="co.yedam.users.service.UserVO">
		insert into users
		VALUES(#{userId},#{userPassword},#{userEmail},#{userName},#{userBirth},#{userPhone},#{userAddress1},#{userAddress2},#{userAddress3},0,'bronze',0)
	</insert>
	<select id="getUser" resultType="co.yedam.users.service.UserVO">
		SELECT*FROM USERS WHERE USER_ID=#{userId} AND USER_PASSWORD=#{userPassword}
	</select>
	
	<select id="userId" resultType="co.yedam.users.service.UserVO">
		SELECT*FROM USERS WHERE USER_ID=#{userId}
	</select>
	
	<update id="editUser" parameterType="co.yedam.users.service.UserVO">
		UPDATE USERS SET USER_PASSWORD = #{userPassword},USER_EMAIL = #{userEmail},USER_NAME = #{userName},USER_PHONE = #{userPhone}, USER_ADDRESS1 = #{userAddress1}, USER_ADDRESS2 = #{userAddress2}, USER_ADDRESS3 = #{userAddress3} WHERE USER_ID = #{userId}
	</update>
	<delete id="delUser" parameterType="string">
		DELETE FROM USERS WHERE USER_ID = #{userId} AND USER_PASSWORD=#{userPassword}
	</delete>
</mapper>